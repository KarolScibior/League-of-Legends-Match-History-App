//types

export const actions = {

};

const initialState = {
  summoner: {}
};

export default rootReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
};